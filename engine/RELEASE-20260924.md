# SageWrite 程序锁版：2026-09-24

- 标签：sagewrite-ps1-20260924。
- 保留原有标签，不移动旧版本。此版本用于定位与恢复，不是不可变服务器保护或数字签名。
- 增加语音写书九步说明、环境恢复清单及无凭据配置示例，README提供入口。
- 纳入04Reference2、04Reference3及测试：核验记录与批准后来源字段修订分离，正文改写仍由04R处理。
- 打包入口改为导出完整仓库版本，包含README、docs和engine；排除历史本机启动器及命令草稿。
- 已跟踪的正式程序、Web代码、依赖声明和安装器随版本保存；旧备份脚本、临时截图、书稿、API密钥、外部运行时和许可证不随包上传。

## 恢复

```powershell
git clone --branch sagewrite-ps1-20260924 --single-branch https://github.com/wenmin20212-dotcom/SageWrite.git SageWrite-20260924
```

先读[环境恢复清单](../docs/ENVIRONMENT-RESTORE.md)，再读[语音写书步骤](../docs/VOICE-WRITING-WORKFLOW.md)与[Agent手册](../docs/AGENT-CALLING-GUIDE.md)。已配置环境可以继续使用；新设备必须自行安装依赖和配置凭据。

发布前验证：32项Python unittest回归测试通过，103个PS1文件AST解析无错误，安装器local模式DryRun通过，git diff空白检查通过；常见凭据格式扫描未发现匹配。ZIP的SHA256随GitHub Release提供。环境清单只记录本机基线，不宣称全新设备已完整验收；未执行收费模型调用或真实平台发布。
