# SageWrite PS1 Lock: 2026-09-17

Release tag: `sagewrite-ps1-20260917`

Release branch: `codex/ps1-lock-20260917`

Previous program lock: `sagewrite-ps1-20260915` (unchanged).

## Scope

This release pins the editorial and publishing PowerShell workflow in `engine`.
Existing 00/01/03, 04B33, 04C44, 04R and other tracked tools remain part of
the repository snapshot. No book manuscripts, book PDFs/EPUBs, local work
directories or untracked artwork are added by this release.

## Changes Since The Previous Lock

- Add 04F deterministic format checking, normalization with backups, and
  input-hash approval verification.
- Route 04-edit heading normalization through 04F and revoke approval if
  other editorial checks fail.
- Require current approval at DOCX, EPUB and PDF publishing entry points.
- Refuse PDF export from stale DOCX when rebuilding fails.
- Share layout rules for preface headings, singleton subheadings, TOC depth
  and reference-group visibility.
- Include format and layout regression tests and operating instructions.

## Verification And Prerequisites

Release verification on 2026-09-17: all 30 regression tests passed; all
top-level PS1 files parsed successfully; staged whitespace checks passed.
The current workflow also generated the complete LTC PDF and EPUB that day.

Before tagging, run all tests with `python -m unittest discover -s tests -v`
from `engine`, parse all top-level PS1 files with the PowerShell parser,
and run `git diff --check`.

The publishing workflow still requires its existing local dependencies,
including Windows PowerShell, Pandoc and Microsoft Word for PDF export.
External models, credentials, installed dependencies and book workspaces
are not frozen by a source-code tag. A tag is a version reference, not
server-enforced immutability; do not force-update or delete a release tag.

## Restore Without Overwriting A Working Copy

```powershell
git clone --branch sagewrite-ps1-20260917 --single-branch https://github.com/wenmin20212-dotcom/SageWrite.git SageWrite-PS1-20260917
```

Use the new checkout for inspection or recovery. Do not reset an existing
working directory containing unfinished work. Future changes should receive
a new commit and a new tag, leaving this release and the earlier lock intact.
