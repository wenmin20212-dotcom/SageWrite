# SageWrite 程序与使用说明锁版：2026-09-18

- 标签：`sagewrite-ps1-20260918`
- 基础版本：`sagewrite-ps1-20260917`
- 范围：仓库中已跟踪的程序及文档完整快照，不包含独立书籍工作区、密钥、外部依赖或本机未跟踪文件。

## 本次变更

本次只补充文档，不修改 PS1 程序行为。README 新增网页与 Agent 对话式创作说明、面向安装 Agent 的迁移与验收规则，以及大模型 API 配置和服务商兼容性边界；快速启动改用仓库相对路径。

当前相关写作与审稿脚本默认使用 `gpt-5.5`，从 `OPENAI_API_KEY` 读取密钥，并请求固定的 OpenAI Responses API 地址。此版本未实现豆包或其他服务商适配，也不是包含所有运行依赖的一键安装包。

## 验证与恢复

本次执行文档差异与空白检查；PS1 程序与基础锁版相同，不宣称重新完成全新电脑安装或模型端到端验收。已有迁移测试的范围与限制记录在 README。

```powershell
git clone --branch sagewrite-ps1-20260918 --single-branch https://github.com/wenmin20212-dotcom/SageWrite.git SageWrite-PS1-20260918
```

原有锁版标签保持不动。此标签用于版本定位与恢复，不等同于服务器端不可变保护；后续修改应创建新提交和新标签，不覆盖本标签。
